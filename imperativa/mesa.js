const alice = [23,82,46] 
const bob = [45,8,32]

function encontrarGanhador (a,b) { 
    if(a[alice] > b[bob]){ 
         alice + 1; 
    } else if( a[alice] < b[bob]){ 
        bob + 1; 
    } else if(a[alice] = b[bob]){ 
        alice && bob + 0
    }
}



console.log("o ganhador é: " + encontrarGanhador(23,45))